# Common requirements for all docker tests.
# Test-specific requirements go in tests/<test>/requirements.txt.
#
# retracesoftware itself is installed from the local checkout by install.sh
# so dockertests exercise the current branch rather than a published wheel.
#
# The Go replay helper is still consumed as a platform-specific binary wheel.
retracesoftware_replay
